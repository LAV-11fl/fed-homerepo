# JavaScript Homework 26

Це простий навчальний приклад базового Gulp проєкту.

## Що зроблено

- У корені репозиторію додано `.gitignore` з `node_modules/`.
- У цій папці є `package.json`.
- У `gulpfile.js` створено власний task `test`.

## Як перевірити

```bash
npm install
npx gulp test
```
