# JavaScript Homework 27

Приклад Gulp task з плагіном `gulp-clean-css`.

## Як перевірити

```bash
npm install
npx gulp styles
```

Після запуску файл `src/style.css` має бути збережений у `dist/style.css` у мініфікованому вигляді.
